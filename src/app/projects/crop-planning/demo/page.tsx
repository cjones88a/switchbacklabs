import CropPlanningDemoClient from "./CropPlanningDemoClient";

export const metadata = {
  title: "Demo: Crop Planning App — Switchback Labs",
};

export default function CropPlanningDemoPage() {
  return <CropPlanningDemoClient />;
}
